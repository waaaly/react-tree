## 1. 节点展开与折叠
- 单击箭头/图标：点击节点前的展开/折叠图标（如 >、▼、+/-）来显示或隐藏子节点。
- 双击节点文本：双击节点标签本身，触发展开/折叠（常见于文件管理器）。
- 全部展开/折叠：通过工具栏按钮或快捷键（如 * 键展开全部）一次性操作所有节点。  
- 展开到指定层级：例如“展开至第3层”或仅展开当前路径上的祖先节点。

## 2. 节点选择
- 单击选中：点击节点文本或区域，高亮显示当前选中项（通常为单选）。

- 多选组合键：按住 Ctrl（Cmd）不连续多选，按住 Shift 连续区域选择（平铺模式下）或范围选择（父子结构需特殊逻辑）。

- 复选框选择：每个节点前带复选框，支持勾选/取消，并可选是否联动父子节点（全选/半选状态）。

- 当前高亮导航：通过键盘上下箭头移动焦点，Space 或 Enter 键切换选中状态。

## 3. 节点增删改
- 内联编辑：双击或按 F2 键，将节点标签转为可编辑输入框，修改名称后确认。

- 添加子节点/兄弟节点：右键菜单提供“新增子节点”、“新增同级节点”选项，弹出表单或内联创建。

- 删除节点：选中后按 Delete 键或右键菜单删除，通常需要二次确认。

- 复制/剪切/粘贴：支持跨树或树内移动/复制节点结构（注意循环引用检查）。

## 4. 拖拽排序与移动
- 节点拖拽：按住节点拖动到目标位置，视觉反馈为占位线或高亮，松开后移动节点。

- 拖拽模式：可以定义为移动（默认）、复制（按住 Ctrl）、链接（不同应用场景）。

- 内部排序：同级节点间拖拽改变顺序，或拖拽成为另一个节点的子节点/父节点。   

- 拖拽边界限制：防止将节点拖入自己或其后代（循环引用校验）。

## 5. 搜索与过滤
- 实时搜索框：输入关键词，高亮匹配节点，并自动展开包含匹配项的路径。

- 仅显示匹配项：隐藏未匹配的节点（过滤模式），或保留结构但灰显不相关节点。

- 搜索导航：支持“下一个匹配”、“上一个匹配”按钮，快速跳转高亮节点。

## 6. 懒加载与动态数据
- 滚动加载：当树高度很大时，滚动到底部自动请求更多同级节点（虚拟滚动常见）。

- 异步展开：点击展开箭头时，发起网络请求加载子节点，并显示 loading 占位。

- 节点刷新：右键提供“刷新”操作，重新加载当前节点的子级数据。

## 7. 辅助与导航
- 键盘导航：

- 上下箭头移动焦点

- 左右箭头展开/折叠（右展开，左折叠）

- Home/End 跳转到首个/最后节点

- 输入字母快速定位到以该字母开头的节点

- 缩进线/连接线：视觉引导线（竖线或直角连线）帮助理解层级关系。

- 虚拟滚动：当节点数量巨大时，仅渲染可视区内的节点，保证滚动性能。

## 8. 上下文菜单（右键菜单）
- 常见功能集合：展开/折叠、新增、编辑、删除、剪切/复制/粘贴、刷新、属性查看等。

- 根据节点类型（文件/文件夹）动态禁用或显示不同菜单项。

## 9. 状态标识
- 图标与颜色：不同节点类型使用不同图标（如文件夹、文件、人员），或通过字体颜色、背景表示状态（只读、禁用、错误）。

- 徽标/角标：显示未读消息数、进度、警告等信息（如组织架构中的人员在线状态）。

## 10. 缩进与层级交互
- 动态缩进：通过缩进大小（通常每级20-30px）直观表达深度。

- 拖拽时缩进变化：拖拽过程中显示目标位置的缩进级别（如成为子级或同级）。    
